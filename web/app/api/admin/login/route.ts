import {createHmac,timingSafeEqual} from "crypto";
import {NextResponse} from "next/server";
function token(email:string){return createHmac("sha256",process.env.ADMIN_SESSION_SECRET!).update(email).digest("hex")}
export async function POST(req:Request){if(!process.env.ADMIN_EMAIL||!process.env.ADMIN_PASSWORD||!process.env.ADMIN_SESSION_SECRET)return NextResponse.json({error:"Admin environment is not configured"},{status:503});let b:any;try{b=await req.json()}catch{return NextResponse.json({error:"Invalid JSON"},{status:400})}const email=String(b.email||"");const password=String(b.password||"");if(email!==process.env.ADMIN_EMAIL||password!==process.env.ADMIN_PASSWORD)return NextResponse.json({error:"Invalid credentials"},{status:401});const res=NextResponse.json({ok:true});res.cookies.set("harsha_admin",token(email),{httpOnly:true,secure:true,sameSite:"strict",path:"/",maxAge:28800});return res}
export function verify(req:Request){if(!process.env.ADMIN_EMAIL||!process.env.ADMIN_SESSION_SECRET)return false;const c=req.headers.get("cookie")?.match(/(?:^|; )harsha_admin=([^;]+)/)?.[1];if(!c)return false;const expected=Buffer.from(token(process.env.ADMIN_EMAIL));const got=Buffer.from(c);return expected.length===got.length&&timingSafeEqual(expected,got)}
