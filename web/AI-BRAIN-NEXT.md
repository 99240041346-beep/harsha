# HARSHA AI Brain — Next Layer

The cloud API now uses a constrained action planner. Future AI integration must return only allowlisted actions such as OPEN_APP, OPEN_SETTINGS, GO_HOME, WEB_SEARCH, and SPEAK.

Never execute arbitrary shell commands, arbitrary URLs, or unrestricted accessibility instructions generated by a model. Sensitive actions should require explicit user confirmation in the Android client.
